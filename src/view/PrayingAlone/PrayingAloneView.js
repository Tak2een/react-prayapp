import PrayingAlone from '../../components/PrayingAlone/PrayingAlone.js';
import {SafeAreaView} from 'react-native';

const PrayingAloneView = () => {
  return (
    <SafeAreaView>
      {/* header */}
      <PrayingAlone />
      {/* footer */}
    </SafeAreaView>
  );
};

export default PrayingAloneView;
